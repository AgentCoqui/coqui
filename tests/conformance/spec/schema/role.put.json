{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://coqui.dev/spec/schema/role.put.json",
  "title": "RolePut",
  "description": "Authoring shape for putRole. The server-owned version token is forbidden — it is assigned on create and incremented on each update; concurrency is expressed via If-None-Match/If-Match headers, not the body. Normative.",
  "type": "object",
  "additionalProperties": false,
  "required": ["name", "access_level"],
  "properties": {
    "name": { "type": "string", "minLength": 1 },
    "access_level": { "$ref": "common.json#/$defs/AccessLevel" },
    "model": { "oneOf": [{ "$ref": "common.json#/$defs/ModelId" }, { "type": "null" }] },
    "toolkits": {
      "type": ["array", "null"],
      "items": { "type": "string" },
      "description": "Implementation-defined allow-list of built-in capability names."
    },
    "max_iterations": { "type": ["integer", "null"], "minimum": 1 },
    "gate": { "type": "boolean", "default": false },
    "instructions": { "type": ["string", "null"], "description": "Role prompt." }
  }
}
